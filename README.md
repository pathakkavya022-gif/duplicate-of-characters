ALORITHM:
STEP 1-> initialize int frq{],char str[];
STEP 3-> using first loop (for(int i=0;str[i]!='\0';i++).
STEP 2-> freq[str[i++]] shows increment of characters.
STEP 3-> use second loop for(int i=0;str[i]!='\0';i++).
STEP 4-> if str[i]>1 then print characters
STEP 5-> str[i]!=0 reset frequency of character
